# GitHub Actions

This folder contains notes about repo workflows.

## Codex workflows
- `codex-request-review`: comments `@codex review` when a PR is labeled `automerge`.
- `codex-automerge`: merges PRs after Codex connector responds "Didn't find any major issues" (or 👍).

Test marker: token-review
Sync marker: trigger-synchronize
